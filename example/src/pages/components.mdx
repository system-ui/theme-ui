import Layout from '../components/layout'
import Note from '../components/note'

export default Layout

# Components

<Note>
  This is a proposal and not implemented yet
</Note>

Other components that *could be* included in this library:

- `Button`
- `NavLink`

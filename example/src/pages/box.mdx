import layout from '../components/layout'
import { Box } from 'theme-ui/layout'

export default layout

# Box Demo

<Box
  px={[3, 4, 5 ]}
  py={6}
  fontSize={5}
  bg='tomato'
>
  Box
</Box>

The Box component includes style props in addition to using the `css` prop

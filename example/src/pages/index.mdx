import Layout from '../components/layout'
import Readme from 'theme-ui/README.md'

export default Layout

<Readme />

## Typography

The output of Typography.js can be added to the `theme.styles` object to style MDX content.
See the [Demo Page](/typography)

## Layout Components

Page layout components and primitive layout components like `Box` and `Flex` could also be included in this library.
See [Layout Components](/layout)

## Other Components

Other useful UI components could be included as well.
See [Components](/components)

## Color

General guidelines for color scheme definition should be documented. See [Color](/color)

import { Theme } from '@theme-ui/css'

import theme from '../src'

// No actual test executed here; just verify it typechecks.

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const typedTheme: Theme = theme

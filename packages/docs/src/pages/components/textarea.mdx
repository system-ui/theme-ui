---
title: Textarea
---

# Textarea

Form textarea component

```js
import { Textarea } from 'theme-ui'
```

```js live=true
<Textarea defaultValue="Hello" rows={8} />
```

## Variants

Textarea variants can be defined in `theme.forms` and the component uses the `theme.forms.textarea` variant by default.

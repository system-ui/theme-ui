---
title: 'Notes'
---

import Layout from '../components/layout'

export default Layout

# Notes

Other considerations for this UI library:

- [ ] Icons
- [ ] Handling webfonts
- [ ] Color modes & `useColorMode` w/ `localStorage`
- [ ] `useDarkMode` - hooks into media query
- [ ] Base UI components
- [ ] POC `gatsby-theme-ui`
- [ ] `Sidebar` component + state

---
title: 'Stack'
---

# Stack Layout

Add vertical space between child elements using CSS Grid.

```jsx
/** @jsx jsx */
import { jsx } from 'theme-ui'

export default ({
  gap = 4,
  ...props
}) =>
  <div
    {...props}
    sx={{
      display: 'grid',
      gridGap: gap,
    }}
  />
```

---
title: 'Typography.js'
---

import TypographyLayout from '../components/typography-layout'
import Lorem from '../components/lorem.mdx'

export default TypographyLayout

# Typography.js Demo

This page uses themes from Typography.js to populate the `theme` object.
Select a theme from the controls above to view a live demo of each one.

## Usage

```sh
npm i @theme-ui/typography
```

Use the `toTheme` function to create a base `theme` object based on Typography.js options or a preset Typography.js theme.

```js
import React from 'react'
import { ThemeProvider } from 'theme-ui'
import { toTheme } from '@theme-ui/typography'
import wp2016 from 'typography-theme-wordpress-2016'

const theme = toTheme(wp2016)

export default props => (
  <ThemeProvider theme={theme}>{props.children}</ThemeProvider>
)
```

_Demo content below_

---

<Lorem />

---
title: Editor
---

import { Box } from 'theme-ui'
import { EditorProvider, Theme, Sx, Styles } from '@theme-ui/editor'
import { TypeScale } from '@theme-ui/style-guide'
import { useReducer } from 'react'
import merge from 'lodash.merge'

import { EditorPackageDemo } from '../../components/editor-demo'

export const reducer = (state, next) => merge({}, state, next)

export const State = (props) => {
  const [value, setValue] = useReducer(reducer, props.value)
  return props.render([value, setValue])
}

# @theme-ui/editor

Components for editing Theme UI theme objects.

```sh
npm i @theme-ui/editor
```

## Demo

<EditorPackageDemo />

## Sx Style Object Editor

<State
  value={{
    color: 'tomato',
    fontSize: 3,
  }}
  render={([style, setStyle]) => (
    <div>
      <pre children={JSON.stringify(style, null, 2)} />
      <Sx.Padding
        value={style}
        onChange={(n) => {
          console.log(style, n)
          setStyle(n)
        }}
      />
      <Sx.Colors value={style} onChange={setStyle} />
    </div>
  )}
/>

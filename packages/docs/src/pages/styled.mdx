---
title: Styled
---

# Styled

As of Theme UI 0.6, `Styled` components dict was renamed to `Themed` to avoid
confusion with `styled` components constructor function from `@emotion/styled`.

[Go to the docs for Themed](https://theme-ui.com/themed).
